{
  "name": "bonkproof",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --check app.js && node --check routebook.js && node --check poi-search.js && node --check poi-projection.js && node --check poi-clustering.js && node --test tests/*.test.mjs"
  }
}
