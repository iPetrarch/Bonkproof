name: Tests

on:
  push:
    branches:
      - main
      - 'chatgpt/**'
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply workload regression updates on development branch
        if: github.ref == 'refs/heads/chatgpt/robust-poi-workloads'
        run: |
          python tools/apply-poi-workload-patch.py
          if git diff --quiet -- tests/ui-regressions.test.mjs; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/ui-regressions.test.mjs
          git commit -m "Update regressions for adaptive POI workloads"
          git push origin HEAD:chatgpt/robust-poi-workloads

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run regression tests
        run: npm test
